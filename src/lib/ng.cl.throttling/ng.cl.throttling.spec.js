describe('ng.cl.throttling', function () {
    'use strict';

    beforeEach(module('ng.cl.throttling'));

    describe('clThrottling', function () {

        describe('default state', function () {

            it('should initialise with known defaults.', inject(function (clThrottling)  {

                //expect(clThrottling.divisor).toEqual(2);
                //expect(clThrottling.rounding).toEqual(false);
            }));

        });
    });
});
